import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  statContainer: {
    marginTop: 16,
  },
  defaulText: {
    color: '#475E75',
    fontSize: 16,
  },
  bgStatView: {
    marginTop: 4,
    width: '85%',
    height: 10,
    borderRadius: 100,
  },
  animatedStat: {
    position: 'absolute',
    left: 0,
    height: 10,
    borderRadius: 100,
  },
});

export default styles;
